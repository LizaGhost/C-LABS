<Window x:Class="lab6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab6"
        mc:Ignorable="d"
        Title="Shop" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="WatermarkTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <TextBox x:Name="textBox" Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock x:Name="watermark"
                                       Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="Gray"
                                       Margin="5,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="11*"/>
            <ColumnDefinition Width="95*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DataGrid Name="itemsDataGrid" Grid.Row="0" Grid.ColumnSpan="3" AutoGenerateColumns="True" />
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="5,5,5,5" Grid.ColumnSpan="2">
            <TextBox Name="filterTextBox" Width="120" Margin="5"  Style="{StaticResource WatermarkTextBox}" Tag="Фильтр" />
            <ComboBox Name="filterFieldComboBox" Width="120" Margin="5" />
            <ComboBox Name="filterCriteriaComboBox" Width="120" Margin="5">
                <ComboBoxItem Content="Содержит"/>
                <ComboBoxItem Content="Не содержит"/>
                <ComboBoxItem Content="Равно"/>
                <ComboBoxItem Content="Не равно"/>
                <ComboBoxItem Content="Больше"/>
                <ComboBoxItem Content="Меньше"/>
            </ComboBox>
            <Button Name="applyFilterButton" Content="Применить фильтр" Margin="5" Click="ApplyFilterButton_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,5">
            <Button Name="summaryButton" Content="Суммарная стоимость" Margin="5" Click="SummaryButton_Click"/>
            <Button Name="countByTypeButton" Content="Количество по типу" Margin="5" Click="CountByTypeButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
